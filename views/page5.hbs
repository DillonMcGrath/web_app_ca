<!DOCTYPE html>
<html>
<head>
  
  <nav class="navbar">
    <div class="logo">
        <a href="/">My Web App</a>
    </div>
    <ul class="nav-links">
        <li><a href="/">Home</a></li>
      
    </ul>
</nav>
  
  <title>Available Cars</title>
  <style>
    /* page styling added to hbs file due to odd results when connected to the css */
    body {
      background-color: #1a1a1a;
      color: #f0f0f0;
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 20px;
    }

    /* Center  heading */
    h1 {
      text-align: center;
      margin-bottom: 20px;
    }

    /* Centers the content in a container */
    .container {
      max-width: 800px;
      margin: 0 auto;
    }

    /* Filter styling */
    form {
      text-align: center;
      margin-bottom: 20px;
    }
    input {
      margin-right: 10px;
      padding: 6px;
      border: 1px solid #444;
      border-radius: 4px;
      background-color: #333;
      color: #f0f0f0;
    }
    button {
      padding: 6px 12px;
      border: none;
      border-radius: 4px;
      background-color: #6c63ff;
      color: #fff;
      cursor: pointer;
    }
    button:hover {
      background-color: #5a52dd;
    }
    
    /* brand name clickable */
    .brand-name {
      cursor: pointer;
      display: block;
      background-color: #333;
      padding: 10px;
      margin-top: 5px;
      border-radius: 4px;
      text-decoration: none;
      color: #f0f0f0;
    }
    .brand-name:hover {
      background-color: #444;
    }

    /* hide model list til clicked */
    .hidden {
      display: none;
    }

    /*  list styling  */
    ul {
      list-style-type: none;
      margin: 0;
      padding-left: 20px; /* shoves models in a small bit */
    }
    li {
      margin: 5px 0;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Available Cars</h1>

    <!-- filter form -->
    <form method="GET" action="/page5">
      <input type="text" name="year" placeholder="Filter by Year" value="{{selectedYear}}">
      <input type="text" name="model" placeholder="Filter by Model" value="{{selectedModel}}">
      <input type="text" name="price" placeholder="Max Price" value="{{selectedPrice}}">
      <button type="submit">Search</button>
    </form>

    <!-- Shows brands and their models -->
    {{#each brands}}
      <div>
        <!-- clickable brand name shows models when clicked -->
        <div class="brand-name" onclick="toggleModels('{{@index}}')">
          {{name}}
        </div>
        <!-- model list  hidden when opening  -->
        <ul id="models-{{@index}}" class="hidden">
          {{#each models}}
            <li>
              <strong>{{year}} - {{name}}</strong> - {{price}}<br>
              {{description}}
            </li>
          {{/each}}
        </ul>
      </div>
    {{/each}}
  </div>

  <script>
    // Toggles the display of models for any given brand
    function toggleModels(index) {
      const list = document.getElementById("models-" + index);
      list.classList.toggle("hidden");
    }
  </script>
</body>
</html>
